-- local function tellMeAbout(number)
--   return match number
--     1 => print("One!"),
--     2 or 5 or 4 or 7 or 11 => print("This is a prime"),
--     -- 13..=19 => print("A teen"),
--     _ => print("Ain't special")
--   end
-- end

-- tellMeAbout(11)

-- public class Language
--   private static DK = "dk"
--   private static EN = "en"

--   public convert(source, language)
--     return match context:getLanguage() do
--       Language.DK => source.Danish,
--       Language.EN => source.English,
--       _ => function()
--         error("???")
--       end
--     end
--   end
-- end

-- local test = {}
-- function test:getLanguage(x)
--   return x
-- end

-- local binary = match true do
--   true => 1,
--   false => 0
-- end

-- print(binary)

-- public class Language
--   private static DK = "danish"
--   private static EN = "english"

--   public convert(source, context)
--     return match context:getLanguage() do
--       Language.DK => source.Danish,
--       Language.EN | _ => source.English
--     end
--   end
-- end

-- local function convert(source, context)
--   return match context.test:getLanguage()
--     Language.DK => source.Danish,
--     Language.EN => source.English,
--     _ => source.English
--   end
-- end

-- local function tellMeAbout(number)
--   return match number
--     1 => do
--       local str = "One!"
--       print(str)
--     end,
--     2 or 5 or 4 or 7 or 11 => print("This is a prime"),
--     _ => print("Ain't special")
--   end
-- end

local pair = { 2, -2 }
match pair
  (x, y) if x == y => print("These are twins"),
  (x, y) if x + y == 0 => print("Antimatter, kaboom!"),
  -- { 2, 3 } => print("ok"),
  _ => print("No correlation...")
end